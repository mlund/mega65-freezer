/* doctest's implementation, compiled once so the test files need only the
 * header.  It carries main(); everything else here is in t_*.cpp. */

#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"
